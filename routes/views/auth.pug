html
	head
		title Authorization
	body
		form(method="post", action="?method=post"）
			label
				span Nickname
				input(name="nickname")
			label
				span Password
				input(name="password")
			button(type="submit") Sign in